/**
 * 업무확인 페이지의 공통 Function
 */

$(document).ready(function() {
	
	/*
	 * 마감기간 조회 포맷 설정
	 */
	$('#startDt').datetimepicker({
		format : 'YYYY-MM-DD',
		useCurrent : true
	});

	$('#endDt').datetimepicker({
		format : 'YYYY-MM-DD',
		useCurrent : true
	});
	
});

